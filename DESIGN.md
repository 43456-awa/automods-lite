# automods-lite 视觉方向（MC 风格）

## 原则
- 灵感来自 Minecraft 游戏 UI 语言，**不复制** AutoMods 站点样式/资源/文案
- 本地可用系统字体，不依赖外链字体 CDN

## 令牌
- 背景：库存深灰 `#1a1a1a` / 面板 `#2a2a2a` / 描边 `#3f3f3f`
- 强调：草方块绿 `#55FF55`（降低饱和到 `#3DDC84` 更耐看）、钻石青 `#5BC0EB`
- 红石警告 `#EF6B73`、金锭 `#F0B429`
- 按钮：硬边 2px 描边 + 底部深色阴影（MC 按钮立体感），无大圆角
- 圆角：4px 或 0（更像素）
- 进度：经验条（绿填充 + 刻度）

## 布局不变
学习双栏 / 工作台聊天 + 资产栏逻辑保持，只换皮。
